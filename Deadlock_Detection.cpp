#include <iostream>
#include <unistd.h>
#include <semaphore.h>      
#include <pthread.h>         


int main() {
    bool deadlock;
    deadlock == false;

    if (deadlock == true) {
        std::cout << "Deadlock Detected";
    }
    
    return 0;
}